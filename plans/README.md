# Implementation Plans

Generated by the improve skill (deep audit) on 2026-07-07, at commit
`9182b14`. Three audit passes (multi-agent fleet + advisor's own
probe-confirmed reads + a second-opinion pass) produced ~45 vetted
findings; the owner selected the sets below and made two scope decisions
that shaped them:

1. **The CLI is dropped** — the direct Bun library is the only supported
   surface (plan 002). Several CLI-level findings are superseded by that
   removal (see the ledger at the bottom).
2. **Owner-stated direction**: task deadlines / task management (plan 012)
   and long-form Markdown + tabular life data (plan 013).

Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done. Design spikes (012–017) produce documents
under `plans/design/` for the owner to ratify — they change no production
code.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Arm CI + native pre-push gate | P1 | S | — | DONE (`advisor/001-arm-the-gates` @ 61ddbd2, reviewed+approved; unmerged) |
| 002 | Drop the CLI (owner decision) | P1 | M | 001 | DONE (`advisor/002-drop-the-cli` @ 3ddb84b, reviewed+approved; baseline now 137 tests/13 files) |
| 003 | Private files: mkdir 0700, chmod 0600, backups too | P1 | S | 001 (002 assumed) | DONE (`advisor/003-private-files` @ 1219dcd, reviewed+approved; 140 tests) |
| 004 | Honest forgetting at the byte level | P1 | M | 003 | DONE (`advisor/004-honest-forget` @ 91996a7, reviewed+approved+independently probed; 141 tests) |
| 005 | Consent-gate validation bypass + verb whitelists | P1 | S | 001 | DONE (`advisor/005-consent-verb-integrity` @ 5cfc581, reviewed+approved+probes replayed; 148 tests) |
| 006 | Crash-safe migrations + real legacy fixtures | P1 | M | 001 | DONE (`advisor/006-migration-durability` @ fda09e5, reviewed+approved+fixtures verified; 152 tests) |
| 007 | Recall starvation fix (surfacing into FTS) | P1 | M | 001 | DONE (`advisor/007-recall-starvation` @ 190b6e0, reviewed+approved+probes replayed incl. cross-plan byte-honesty; 156 tests) |
| 008 | Library hardening batch (registry JSON, propsPatch, backup guards) | P2 | S–M | 005 | DONE (`advisor/008-library-hardening` @ 7c51c3f, reviewed+approved+probes replayed; 164 tests) |
| 009 | Packaging truth (.gitattributes, stray files, tag backfill) | P2 | S | 002 | DONE (`advisor/009-packaging-truth` @ 79a0a6e, reviewed+approved+archive verified; 4 local tags created — owner to push or delete) |
| 010 | Docs sync (counts, versions, map, error contract) | P2 | S | 002 | DONE (`advisor/010-docs-sync` @ 5b0a7bb, reviewed+approved) |
| 011 | Strict conformance runner + shared test fixture | P2 | S–M | 005, 007, 008 | DONE (`advisor/011-conformance-strengthening` @ b76a971, reviewed+approved+negative-verified) |
| 012 | SPIKE: task deadlines & task management | P2 | M | — (006 for any build) | DONE (design doc `plans/design/task-arc.md`, reviewed; 6 owner decisions AWAITING RATIFICATION) |
| 013 | SPIKE: long-form MD + tabular life data | P2 | M | — (006 for any build) | DONE (design doc `plans/design/life-documents.md`, reviewed; 7 owner decisions AWAITING RATIFICATION) |
| 014 | DoctorReport revision (3 documented IOUs) | P2 | S–M | 001 | DONE (`advisor/014-doctor-revision` @ 005da77, reviewed+approved+probes replayed; 169 tests — THE CHAIN TIP to merge) |
| 015 | SPIKE: read-only open mode | P3 | S–M | — | DONE (design doc `plans/design/readonly-open.md`, reviewed; 6 owner decisions AWAITING RATIFICATION) |
| 016 | SPIKE: export/portability + restore (library verbs) | P3 | M | 002 | DONE (design doc `plans/design/export-restore.md`, reviewed; owner decisions AWAITING RATIFICATION) |
| 017 | SPIKE: multi-window edge validity | P3 | S–M | — (006 for any build) | DONE (design doc `plans/design/multiwindow-validity.md`, reviewed; recommends BUILD Design B — owner decisions AWAITING RATIFICATION) |

**Execution complete 2026-07-08.** All 17 plans executed and reviewed. Code
chain: `advisor/001` … `advisor/014-doctor-revision` @ `005da77` (12 stacked
commits on `9182b14`, fast-forwardable, 169 tests green). Merge tip:
`git merge advisor/014-doctor-revision`. After merging: arm the hook
(`git config core.hooksPath .githooks`), decide the 4 backfilled tags
(push or `git tag -d`), and ratify the 5 design docs in `plans/design/`.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first, always** — it is the verification gate every other plan's
  done-criteria lean on.
- **002 before 009/010/016** — the shipped tree, the docs, and the
  export-verbs design all depend on the CLI being gone.
- **003 before 004** — both edit the same `Store.open` region; 004's
  excerpts assume 003's shape.
- **005 before 008 and 011** — same files (008); the strict runner (011)
  should judge scenarios added by 005/007/008, and 005 removes known
  raw-SQLite-error escapes the strict runner would trip on.
- **006 before any v5 migration** a spike (012/013/017) may propose —
  transactional deltas are the precondition for touching the schema again.
- **012 and 017 cross-reference**: re-blocked tasks are the same
  multi-window pattern; read both design docs together before ratifying
  either.
- **015 and 016 compose**: exports are pure reads; if `ReadStoreContract`
  lands, export verbs belong on it.

## Verification quick reference

`bun install` · `bun run check` (= `bunx tsc --noEmit && bunx biome check .
&& bun test`) · baseline at planning time: 155 tests / 14 files green
(139 / 13 after plan 002).

## Findings considered and rejected (do not re-audit)

- **Vector per-query copies** (`allVectors` `.slice()` per row): by-design-
  adjacent GC cost at personal scale; revisit only if vector recall becomes
  a hot loop.
- **`suggestIdentities` O(N²)**: explicitly owner/host-scheduled batch;
  fine at personal-type cardinality.
- **`created`/`derivations` indexes**: no demonstrated pain; fold into
  whichever v5 migration lands first (012/013/017 build), not standalone.
- **spine.ts split** (history/type-registry extraction): opportunistic
  only — the choke-point coupling is deliberate (I10/I12 by construction).
- **render.ts type-guards, agenda/episode duplication, CLI default
  mismatches, CLI barrel imports**: superseded by plan 002 (CLI removed).
- **`--field` coercion crash, `suggest-identities` cap=0, `--now` lenient
  parse, CLI display gate for quarantined content**: probe-confirmed CLI
  bugs, superseded by plan 002. If the CLI is ever revived from git
  history, fix these first — they are recorded in the audit.
- **Day-anchor scan on every create** (`ensureDayNode` json_extract scan):
  real but sub-ms for years; plan 013's bulk-import design addresses the
  aggravated case; a Ctx-level memo is the cheap fix if it ever shows up
  in a profile.
- **`MemoryError` code taxonomy** (`conflict` carries ~10 meanings): real
  API-design debt, deliberately unplanned this round — it is a breaking
  change that should ride a minor version WITH a consumer; recorded for
  the next audit.
- **Normalized-title lookup seam** (4 divergent implementations + N+1):
  real; deferred because plans 005/007 touch the same regions first and a
  consolidation mid-flight would conflict. Natural follow-up after 011.
- **`edgesOf(id)` public read** (closeEdge needs ids no read supplies):
  folded into plan 012's design as a prerequisite — not separately
  planned.
- **`engines.bun ">=1.2"` floor unverified**: handled inside plan 001 (CI
  matrix leg, continue-on-error).
- **Dead `AuditEntry` export / `isUlid` test-only export**: cosmetic;
  bundle into whichever plan next touches `src/index.ts`.
- **FTS `extra` convention in 3 places**: real drift risk; deferred —
  plan 007 rewrites two of the three sites anyway; re-check after it
  lands.
- **Literal NUL byte in `src/doctor.ts:101`** (found by spike 016,
  verified pre-existing since before the audit): a deliberate dedup-key
  separator written as a raw byte instead of the `" "` escape —
  behavior-identical fix, one byte, but until fixed grep treats the file
  as binary and silently skips it. Fold into whichever plan next touches
  doctor.ts.
- **Test-fixture duplication ×11**: planned — inside 011 (Step 3), not a
  separate plan.

## Audit provenance

- Fleet pass (6 categories) + advisor probe-confirmed pass + Fable 5
  second-opinion pass (correctness/debt/deps-dx/direction with
  AGREE/DISAGREE protocol). All findings in these plans were verified
  against the code by the advisor before planning; probe scripts lived in
  the session scratchpad and their decisive results are restated inside
  each plan's "Why this matters".
